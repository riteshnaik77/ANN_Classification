create virtual env--> activate cenv-->
create requirements.txt-->
upgrade pip using command,  "python.exe -m pip install --upgrade pip"
pip install -r requirements.txt
pip install ipykernel

init git & Push to git